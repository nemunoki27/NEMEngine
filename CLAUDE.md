# GEMINI.md

## 基本方針

このプロジェクトでは C++ コードを扱う。
作業時は、既存のコードベースの書き方を最優先で尊重すること。

## 禁止事項

- Git 操作は絶対に行わないこと。
  - `git pull`
  - `git push`
  - `git commit`
  - `git reset`
  - `git checkout`
  - `git merge`
  - `git rebase`
  - その他、リポジトリの状態を変更する可能性がある Git コマンド
- Git 操作が必要に見える場合でも、実行せず、必ずユーザーに確認すること。
- 判断に迷うコマンド、削除・上書き・外部送信を伴う操作は、実行前に必ずユーザーに確認すること。

## コーディング規約

- 既存の実装をよく読んでから作業すること。
- AI に書かせたように見えるコードにしないこと。
- 新しいコードを書く場合は、以下を既存コードに合わせて模倣すること。
  - 変数名の付け方
  - 関数名の付け方
  - クラス名・構造体名の付け方
  - ファイル分割の方針
  - インデント
  - 空行の入れ方
  - コメントの書き方
  - 条件分岐やループの書き方
  - エラー処理の書き方
  - 既存の設計思想
- 既存コードのスタイルと異なる書き方を勝手に導入しないこと。
- 必要以上に大きなリファクタリングをしないこと。
- 指示された範囲を超えて、関係ないファイルや処理を変更しないこと。

## コメント方針

- コメントは多めに入れること。
- ただし、AI が書いたように見える不自然なコメントは書かないこと。
- 当たり前の処理を説明するだけのコメントは避けること。
- コメントは、既存コードの雰囲気に合わせること。
- コメントでは、次のような内容を優先して説明すること。
  - なぜその処理が必要なのか
  - 仕様上の注意点
  - 境界条件
  - 後から読んだ人が迷いやすい箇所
  - 既存処理との関係
- 過度に丁寧すぎる文章、チュートリアル風の説明、AI らしい一般論は避けること。

## 作業の進め方

- 変更前に、まず対象ファイルと修正方針を確認すること。
- 既存コードを読まずに新規実装を始めないこと。
- 修正する場合は、最小限の変更で目的を達成すること。
- 変更後は、可能な範囲で影響範囲を説明すること。
- テストやビルドを実行する必要がある場合は、実行するコマンドを先に説明すること。
- 実行してよいか判断できないコマンドは、ユーザーに確認してから実行すること。

## 判断に迷った場合

以下の場合は、必ず作業を止めてユーザーに確認すること。

- どう実装すべきか判断できない場合
- 仕様が曖昧な場合
- 複数の実装方針があり、どれを選ぶべきか分からない場合
- 既存コードの意図が読み取れない場合
- 変更範囲が当初の想定より広がりそうな場合
- Git 操作が必要に見える場合
- 破壊的なコマンドやファイル削除が必要に見える場合
- 外部通信、パッケージ追加、環境変更が必要に見える場合

判断に迷ったまま作業を続けてはいけない。
必ずユーザーに質問し、回答を得てから作業を再開すること。

## 出力・説明の方針

- 回答は日本語で行うこと。
- 実装方針、変更内容、注意点は簡潔に説明すること。
- コードの説明では、既存コードとの関係を重視すること。
- 不確かなことを断定しないこと。
- 勝手に仕様を補完しないこと。
