# Claude Code 共通作業ルール

このファイルは、Claude Code CLI で `NEMEngine` の開発を行うときに常に守る共通ルールです。  
個別の作業指示Markdownがある場合も、この共通ルールを優先して確認し、矛盾がある場合は作業前に確認してください。

---

## 1. 作業対象ディレクトリ

作業は必ず以下のディレクトリ内で行ってください。

```text
C:\Users\k023g\school\NEMProjects\NEMEngine
```

`MyGame/` は確認対象外です。  
指示がない限り、`MyGame/` 配下のファイルは読まない、編集しない、参照しないでください。

---

## 2. 既存実装の模倣

AIに書かせたと思われるコードにならないよう、必ず既存実装を確認してから作業してください。

特に以下を既存コードに合わせてください。

- 変数名
- 関数名
- クラス名
- ファイル分割
- include の並び
- コメントの書き方
- コメントの量
- 空行の入れ方
- 改行位置
- 関数の粒度
- 早期returnの使い方
- namespace の使い方
- エラー処理、ログ出力、ステータスメッセージの文体

新しい実装を追加する場合も、周辺コードの書き方をよく見て、同じ開発者が書いたように自然に馴染ませてください。  
不要に説明的なコメントや、AIが生成したような過剰なコメントは追加しないでください。

---

## 3. 実装時に常に意識すること

コードを書くときは、常に以下を意識してください。

- 汎用性
- 拡張性
- 処理速度
- 既存機能との整合性
- 既存ヘルパーや既存クラスの再利用
- 処理の重複回避
- 将来的な他ツールへの流用

特にNodeGraph系の処理は、RenderPathGraphTool専用に閉じすぎないようにしてください。  
他のエディタツールでも利用される可能性があるため、汎用NodeGraph処理とRenderPath専用処理はできるだけ分離してください。

---

## 4. Git操作の禁止

Git操作は勝手に行わないでください。

禁止する操作の例:

```text
git push
git pull
git commit
git merge
git rebase
git reset
git checkout
git switch
```

Gitの状態確認が必要な場合でも、勝手に変更を伴う操作は行わないでください。  
ユーザーから明示的な指示がある場合のみ実行してください。

---

## 5. 指示Markdownの遵守

個別の作業指示Markdownが渡された場合、その内容を必ず守ってください。

作業前に以下を行ってください。

1. 指示Markdownを読む
2. 対象ファイルと関連ファイルを確認する
3. 既存実装の設計、命名、コメント量、改行を確認する
4. 既存機能で使い回せる処理がないか確認する
5. 必要最小限の変更範囲を決める
6. 実装する

指示Markdownに優先度が書かれている場合は、優先度の高い項目から順に対応してください。

---

## 6. コード編集後の確認

コード編集後は、必ず最終確認を行ってください。

確認項目:

- 意図しないファイルを編集していないか
- `MyGame/` 配下を編集していないか
- 既存の命名、コメント、改行、書き方に合っているか
- 不要なデバッグコードが残っていないか
- 一時ファイルを作成していないか
- include 不足がないか
- 同じ処理を重複実装していないか
- 汎用処理と専用処理が混ざりすぎていないか
- ビルド対象に必要な新規ファイルが含まれる構成になっているか

---

## 7. Premake 実行

コード編集後、最終確認を行ったあとに、以下のbatを起動してください。

```text
Premake/generate_vs2026.bat
```

実行場所はプロジェクトルートを基準にしてください。

```text
C:\Users\k023g\school\NEMProjects\NEMEngine
```

Premake実行後は、生成結果に問題がないか確認してください。  
実行できなかった場合は、実行できなかった理由を明確に報告してください。

---

## 8. 報告時のルール

作業完了時は、以下を簡潔に報告してください。

- 変更したファイル
- 変更内容の概要
- 指示Markdownのどの項目に対応したか
- Premakeを実行したか
- ビルド確認を行ったか
- 未確認または未対応の項目がある場合はその理由

曖昧な成功報告はしないでください。  
実行していない確認を、実行したように書かないでください。
